// Empty stub file - keeping to prevent import errors
export const syncQueue = null;

// Function to check Supabase connection - simplified to always return false
export const checkSupabaseConnection = async (): Promise<boolean> => {
  return false;
};

// Function to sync data to Supabase - simplified to always return false
export const syncDataToSupabase = async (): Promise<boolean> => {
  return false;
};
